# Матрица критериев хакатона

Это рабочий чек-лист, а не заявление о полученных баллах. Он связывает каждый критерий с проверяемым материалом и показывает, что ещё нужно закрыть до сдачи.

| Критерий | Что уже можно показать | Что остаётся до финала |
|---|---|---|
| Понимание задачи и тема, 10 | Один ясный job: найти новый билет после срыва рейса и прибыть до дедлайна; исходный рейс и альтернативы разделены | Подкрепить питч цифрами продуктового исследования |
| Глубина функционала, 15 | Дедлайн, бюджет, транспорт, багаж, пассажиры, до 3 вариантов, пустой результат; Support MVP с 21 проверенной сущностью и тремя intent-actions | Подключить реальные предложения всех доступных видов транспорта через Tutu MCP |
| UX/UI, 20 | Четыре коротких экрана; первый содержит ровно 3 поля; введённые данные видны и сохраняются при возврате; запас до дедлайна доминирует в вертикальной выдаче; есть risk, empty, nearest-late и отдельная помощь | Провести короткий коридорный тест с новым пользователем и исправить найденные затруднения |
| Презентация, 5 | Сквозной демо-кейс, фирменная визуальная система и social-preview | Собрать 10-минутный питч и запасной видеосценарий |
| Документация, 10 | README, пользовательская инструкция, архитектура, предметный словарь, критерии и команды проверки | После live-интеграции добавить точные MCP-инструменты и примеры ответов |
| Инновационность, 10 | Антикризисный deadline-first поиск поверх нескольких видов транспорта; LLM отделён от критической логики | Добавить подтверждаемое извлечение параметров из свободного текста и grounded-объяснение результатов |
| Стабильность, 10 | Серверная Zod-валидация, чистые search/support engines, явный no-results, безопасный directory miss, воспроизводимый fixture, production build, 12 интеграционных тестов | Обработать таймауты и частичный отказ реального MCP, добавить серверную аналитику |
| Архитектура, 10 | Слои UI → API → provider → normalized offers → deterministic selection; источник заменяется через порт | Реализовать Tutu MCP adapter и контрактные тесты его нормализации |
| Качество кода, 10 | TypeScript strict, изолированные доменные типы, ESLint без ошибок, интеграционные тесты | Добавить unit-тесты нормализатора реальных ответов MCP после фиксации схемы |

## Обязательные артефакты сдачи

- доступная ссылка на работающий прототип;
- пользовательская документация;
- репозиторий с документацией к коду;
- презентация, где демонстрация выполняется на самом прототипе;
- проверенный запасной сценарий на случай сетевого сбоя Tutu MCP.
